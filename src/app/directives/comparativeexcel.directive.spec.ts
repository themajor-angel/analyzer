import { ComparativeexcelDirective } from './comparativeexcel.directive';

describe('ComparativeexcelDirective', () => {
  it('should create an instance', () => {
    const directive = new ComparativeexcelDirective();
    expect(directive).toBeTruthy();
  });
});
